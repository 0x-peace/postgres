---
pg_service: "postgresql"

pg_data: "/var/lib/postgresql/{{ postgresql_version }}/main"
pg_conf: "/etc/postgresql/{{ postgresql_version }}/main"

pg_daemon: "postgresql@{{ postgresql_version }}-main.service"

pg_stats_temp_directory: '/var/run/postgresql/{{ postgresql_version }}-main.pg_stat_tmp'
